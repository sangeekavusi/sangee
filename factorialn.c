#include<stdio.h>
{
int n,i;
printf("enter an integer");
scanf("%d"),&n);
if(n<0)
printf("negative number doesn't exist");
else
{
for(i=1;i<=n;i++)
factorial*=i
}
printf("factorial of%d==11% n,factorial");
return 0;
}
}
}
