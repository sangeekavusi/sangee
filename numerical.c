#include<stdio.h>
#include<string.h>
float square(float x);
int main()
{
float m,n;
printf("\n enter the some number for finding sequence\n");
scanf("%f",&m);
n=square(m);
printf("\n square of he given no%f  is %f",m,n);
{
float p;
p=x*x;
return(p);
}
